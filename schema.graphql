type User {
  id: Int
  email: String!
  name: String!
  posts: [Post]
}

type Post {
  id: Int
  title: String!
  body: String!
  author: User
  authorId: Int
}

type Query {
  users: [User!]!
  user(id: Int!, name: String): User
  posts: [Post]
}
